import {
    HouseIcon,
    InfoIcon
} from "phosphor-react-native";

export const HomeIcon = (props: any) => {
    return <HouseIcon size={24} color="#000" {...props} />;
}

export const AboutIcon = (props: any) => {
    return <InfoIcon size={24} color="#000" {...props} />;
}