import { Text } from "@/components/ui/text"
import { SafeAreaView } from "react-native-safe-area-context"

export default () => {
    return (
        <SafeAreaView>
            <Text>Post</Text>
        </SafeAreaView>
    )
}